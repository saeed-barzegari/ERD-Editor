import {Element} from "@/components/erd/basic/element";

export class HorizontalLine extends Element {
    constructor() {
        super();
    }

    layout() {
        return
    }

    draw(context: CanvasRenderingContext2D) {
        super.draw(context);
    }
}